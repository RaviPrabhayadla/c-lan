#include<stdio.h>
void main()
{
    int a;
    int sum=0;
    while(1)
    {
        printf("Enter a number:");
        scanf("%d",&a);
        if(a==0)
           break;
        else
          sum=sum+a;
        printf("The sum of numbers is %d",sum);
    }
}
