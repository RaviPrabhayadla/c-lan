#include <stdio.h>
int sod(int n);
int main()
{
    int a;
    printf("Enter the number: ");
    scanf("%d", &a);
    printf("Sum of digits in %d is %d\n",a, sod(a));
    return 0;
}
int sod(int n)
{
    if (n!= 0)
    {
       return (n%10+sod(n/10));
    }
    else
    {
       return 0;
    }
}