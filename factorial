#include<stdio.h>
int fact(int n);
int main()
{
    int a;
    printf("Enter a num:");
    scanf("%d",&a);
    printf("\n The factorial of %d is %d",a,fact(a));
    return 0;
}
int fact(int n)
{
    if(n==1)
      return 1;
    else
    {
        n*=fact(n-1);
        return n;
    }
}