#include <stdio.h>

void main()
{
    int a,b=0,c=1,d,i;
    printf("enter the number of terms: ");
    scanf("%d", &a);
    printf("fibonacci series: %d, %d", b, c);
    for (i=3;i<=a; i++)
    {
        d = b + c;
        printf(", %d", d);
        b= c;
        c= d;
    }
}